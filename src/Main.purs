module Main where

import Prelude (Unit, discard)

import Effect (Effect)
import Effect.Class.Console (log)


main :: Effect Unit
main = do
  log "Neodoc does not yet provide a command line interface."
  log "Check out github.com/felixSchl/neodoc/issues/101 for more information."
